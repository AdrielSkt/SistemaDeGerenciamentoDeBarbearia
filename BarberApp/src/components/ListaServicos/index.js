import React from 'react';
import { View } from 'react-native';
import { useNavigation } from '@react-navigation/native';

export default function ListaServicos({servicos}){
  const navigation = useNavigation();
  return(
    <View>
        
    </View>
  )
}